name: "Rust Security Hardened"

queries:
  - uses: security-and-quality
  - uses: security-extended
  - uses: ./.codeql/queries/security-hardening.qls

paths-ignore:
  - target/**
  - benches/**
  - examples/**
  - "**/*.md"

paths:
  - src/